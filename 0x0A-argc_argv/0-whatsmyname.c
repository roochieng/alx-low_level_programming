#include "main.h"
#include <stdlib.h>
/**
 * main - main funtion that prints itself
 * @argv: arguments
 * @argc: argument idexes
 * Return: 0
 */
void main(int argc, char *argv[])
{
	if (argc > 0)
	{
		printf("%s\n", argv[0]);
	}
	return (0)
}
