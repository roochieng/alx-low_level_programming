#include <stdio.h>
#include "main.h"
/**
 * main - check code
 *
 * Return: returns 0
 */
int main(void)
{
	char c;

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
