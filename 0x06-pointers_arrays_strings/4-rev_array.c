#include "main.h"
/**
 * reverse_array - Reverses the array
 * @a: Array
 * @n: number of elements
 * Return: reversed
 */
void reverse_array(int *a, int n)
{
	int reversed;

	for (n > -1; a[n] != '\0'; n--)
	{
		revered[n] = s[n];
	}
	reversed[n] = '\0';
	return (reversed);
}
